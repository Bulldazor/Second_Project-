<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rr.cgpacounter">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActvty">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ClaculatorActvty" />
        <activity android:name=".MainActivity3" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".InfoActvty"
            android:label="@string/title_activity_info_actvty"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".LastAcvty"></activity>
    </application>

</manifest>